Check if the browser supports geolocation.

Set options for high accuracy, a 5 sec timeout and no caching.

Use watch position to check users location contiounously.

Emit the latitude and longitude via socket with "send-location", log any error to the console

Initialised a map centered at cordinate (0,0) with a zoom level of 15 with leaflet.

Add open streetmap tiles to the map.

Create an empty object marker.

When recieving the location data via socket, extract id, latitude and longitude and center the map on the new coordinates.

If a marker for the id exist update its position otherwise create a new marker at agiven coordinates and add it.
